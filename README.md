# backend for open-timetrack

## install with helm

```shell
helm repo add blueprint https://r-vanooyen.github.io/maven-docker-helm-blueprint/
helm repo update
helm install tmp blueprint/maven-docker-helm-blueprint
```
